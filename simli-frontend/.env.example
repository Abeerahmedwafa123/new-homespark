# Base URL of the Simli Laravel backend (no trailing slash).
# Standalone dev default (php artisan serve runs on :8000).
# Leave empty ("") when the frontend is merged same-origin into the LMS.
VITE_API_BASE_URL=http://localhost:8000
