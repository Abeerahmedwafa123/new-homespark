# public/audio

Place the sample audio here as `test.mp3` (referenced by default in `SimliAvatar.vue`).

The binary file is intentionally not committed to the repo. Any audio file works — it is
resampled to 16 kHz mono PCM16 in the browser before streaming to Simli. You can also point
the input field at any CORS-enabled audio URL instead.
